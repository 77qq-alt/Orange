spring:
  application:
    name: orange-api
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension:  yaml
        refresh-enabled: true
      discovery:
        server-addr: 127.0.0.1:8848
  profiles:
    active: dev

